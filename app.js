const bookList = document.querySelector('#book-list');

console.log('the parents node is:', bookList.parentNode);
console.log('the parents element is:', bookList.parentElement.parentElement);

console.log(bookList.children);